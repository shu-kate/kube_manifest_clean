apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      imagePullSecrets:           # ← ADD THIS
      - name: regcred              # ← ADD THIS
      containers:
      - name: myapp
        image: kateshubhangi/todo-app:build-7
        ports:
        - containerPort: 80
