apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      # Remove imagePullSecrets section completely
      containers:
      - name: myapp
        image: kateshubhangi/todo-app:build-7
        ports:
        - containerPort: 80
